# Telegram Бот с Админ-панелью для VectorStom

## 🎯 Обзор

Telegram бот с веб админ-панелью для управления доступом пользователей к системе RAG на базе Docling.

### Основные возможности

✅ **Telegram Бот:**
- Авторизация пользователей по Telegram ID
- Поиск по документам с учетом истории чата
- Кнопка "Показать источники" после каждого ответа
- Команды: /start, /help, /clear

✅ **Веб Админ-панель:**
- Добавление пользователей по номеру телефона
- Просмотр списка пользователей
- Статистика: пользователи, запросы за день/неделю
- Логи всех запросов
- Управление документами

✅ **База данных SQLite:**
- Таблица users: управление пользователями
- Таблица query_logs: логирование всех запросов
- Автоматическое создание при старте

## 📦 Установка

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например, "VectorStom Bot")
4. Введите username бота (например, "vectorstom_bot")
5. Скопируйте токен бота (выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env.example`:

```bash
cp .env.example .env.local
```

Заполните обязательные переменные:

```env
# Polza.ai API (для DeepSeek)
POLZA_API_KEY=ваш_polza_api_key

# Telegram Bot Token
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# Database (можно не менять)
DB_PATH=/db/docling.db

# Flask API URL (можно не менять)
FLASK_API_URL=http://webapp:5000
```

### 3. Запуск системы

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка логов
docker-compose logs -f telegram-bot
docker-compose logs -f webapp
```

## 🚀 Использование

### Для Администраторов

#### 1. Откройте админ-панель

Перейдите по адресу: **http://localhost:5000/admin**

#### 2. Добавьте пользователя

1. В форме "Добавить пользователя" введите номер телефона в международном формате
   - Пример: `+79991234567`
2. Нажмите "Добавить пользователя"
3. Пользователь будет добавлен в базу

#### 3. Сообщите пользователю

Отправьте пользователю ссылку на бота и его Telegram ID для привязки:
- Ссылка на бота: `https://t.me/ваш_bot_username`
- Пользователь может узнать свой Telegram ID через [@userinfobot](https://t.me/userinfobot)

### Для Пользователей

#### 1. Запуск бота

1. Откройте бота в Telegram
2. Нажмите кнопку "START" или отправьте `/start`
3. Если вы авторизованы - увидите приветствие
4. Если нет - получите сообщение с вашим Telegram ID для передачи администратору

#### 2. Задавайте вопросы

Просто пишите вопросы боту, как в обычном чате:
```
Что такое нормочас?
Как рассчитать ВВ клиники?
```

#### 3. Просмотр источников

После каждого ответа появляется кнопка **"📄 Показать источники"**
- Нажмите её, чтобы увидеть из каких документов был сформирован ответ

#### 4. Команды бота

- `/start` - начать работу / проверить авторизацию
- `/help` - показать справку
- `/clear` - очистить историю чата

## 🛠️ Архитектура

```
┌─────────────────┐
│  Telegram User  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐      ┌──────────────┐
│  Telegram Bot   │─────▶│  Flask API   │
│   (aiogram 3)   │      │  (webapp)    │
└─────────────────┘      └──────┬───────┘
                                │
                                ▼
                    ┌──────────────────────┐
                    │   SQLite Database    │
                    │  (users, logs)       │
                    └──────────────────────┘
                                │
                                ▼
                    ┌──────────────────────┐
                    │  Qdrant (vectors)    │
                    │  + Ollama (embed)    │
                    │  + DeepSeek (LLM)    │
                    └──────────────────────┘
```

## 📊 Админ-панель

### Разделы

1. **Пользователи** - управление доступом
   - Добавление по номеру телефона
   - Просмотр списка (ID, телефон, Telegram ID, статус)
   - Деактивация/удаление

2. **Статистика** - метрики системы
   - Всего пользователей
   - Активных за неделю
   - Запросов сегодня/за неделю
   - Всего запросов

3. **Логи запросов** - история
   - Пользователь (username, телефон)
   - Текст запроса
   - Время запроса

## 🔐 Безопасность

### Текущий уровень (Development)

- ✅ Авторизация через Telegram ID
- ✅ Проверка доступа перед каждым запросом
- ✅ Логирование всех действий
- ⚠️ Админ-панель без защиты (доступна всем)

### Рекомендации для Production

1. **Защита админ-панели:**
   ```bash
   # Добавьте Basic Auth через Nginx или Flask-HTTPAuth
   pip install Flask-HTTPAuth
   ```

2. **SSL/TLS сертификаты:**
   ```bash
   # Используйте Let's Encrypt для HTTPS
   docker-compose -f docker-compose.prod.yml up -d
   ```

3. **Rate Limiting:**
   - Ограничение запросов на пользователя (например, 10 в минуту)
   - Защита от DDoS

4. **Резервное копирование:**
   ```bash
   # Бэкап SQLite базы
   docker exec docling-webapp sqlite3 /db/docling.db .dump > backup.sql
   
   # Восстановление
   docker exec -i docling-webapp sqlite3 /db/docling.db < backup.sql
   ```

## 🐛 Отладка

### Проверка логов

```bash
# Логи бота
docker-compose logs -f telegram-bot

# Логи Flask API
docker-compose logs -f webapp

# Все логи
docker-compose logs -f
```

### Частые проблемы

**1. Бот не отвечает**
```bash
# Проверьте токен в .env.local
docker exec docling-telegram-bot env | grep TELEGRAM_BOT_TOKEN

# Перезапустите бота
docker-compose restart telegram-bot
```

**2. Ошибка "Доступ запрещен"**
- Убедитесь, что пользователь добавлен в админ-панели
- Проверьте, что Telegram ID совпадает

**3. База данных не создается**
```bash
# Проверьте права на папку
ls -la shared/db/

# Создайте вручную
mkdir -p shared/db
docker-compose restart webapp
```

**4. API не отвечает**
```bash
# Проверьте, что webapp запущен
docker ps | grep webapp

# Проверьте подключение
curl http://localhost:5000/health
```

## 📝 Структура проекта

```
.
├── webapp/                 # Flask приложение
│   ├── app.py             # Главный файл с API
│   ├── database.py        # Работа с SQLite
│   ├── admin_routes.py    # API админ-панели
│   └── templates/
│       ├── index.html     # Веб-интерфейс чата
│       └── admin.html     # Админ-панель
│
├── telegram_bot/          # Telegram бот
│   ├── bot.py            # Главный файл бота
│   ├── handlers.py       # Обработчики команд
│   ├── keyboards.py      # Inline-клавиатуры
│   ├── Dockerfile        # Docker образ бота
│   └── requirements.txt  # Зависимости бота
│
├── shared/               # Общие данные
│   └── db/              # SQLite база
│       └── docling.db   # База пользователей
│
├── docker-compose.yml    # Docker конфигурация
└── .env.local           # Переменные окружения
```

## 🔄 Обновление

```bash
# Остановка системы
docker-compose down

# Обновление кода
git pull

# Пересборка образов
docker-compose build telegram-bot webapp

# Запуск
docker-compose up -d
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что все сервисы запущены: `docker-compose ps`
3. Проверьте `.env.local` на наличие всех обязательных переменных
4. Попробуйте перезапустить: `docker-compose restart`

## 🎉 Готово!

Теперь у вас есть полнофункциональный Telegram бот с админ-панелью для управления доступом к системе RAG на базе Docling!

---

**Версия:** 1.0.0  
**Дата:** 27.01.2026
