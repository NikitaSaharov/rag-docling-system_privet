# 🎨 Парсер стилей веб-сайтов

Инструмент для автоматического извлечения CSS стилей, цветов и шрифтов с веб-сайтов.

## 📦 Установка

```bash
pip install -r requirements.txt
```

## 🚀 Использование

```bash
python parse_styles.py
```

Скрипт автоматически:
1. Загрузит страницу https://globaldent.group/
2. Извлечет все CSS файлы
3. Проанализирует цвета, шрифты и размеры
4. Создаст 2 файла с результатами

## 📄 Результаты

После выполнения будут созданы:

### `styles_analysis.json`
Полный JSON отчет с анализом:
- Все найденные цвета (HEX, RGB, RGBA)
- Все шрифты (font-family)
- Все размеры шрифтов
- Статистика использования

### `extracted_styles.css`
Готовый CSS файл с извлеченными стилями:
- CSS переменные для топ-10 цветов
- CSS переменные для топ-5 шрифтов
- Можно напрямую использовать в проекте

## 🎯 Пример вывода

```
🎨 ЦВЕТА (найдено уникальных: 45)

Топ-10 наиболее используемых:
  #00A651              - использован 127 раз
  #ffffff              - использован 98 раз
  #000000              - использован 54 раз
  ...

🔤 ШРИФТЫ (найдено уникальных: 8)

Наиболее используемые:
  'Montserrat', sans-serif - использован 43 раза
  'Roboto', Arial          - использован 21 раз
  ...
```

## 🔧 Настройка

Чтобы спарсить другой сайт, измените URL в файле `parse_styles.py`:

```python
target_url = "https://your-website.com/"
```

## 📝 Возможности

- ✅ Извлечение inline CSS
- ✅ Загрузка внешних CSS файлов
- ✅ Парсинг HEX, RGB, RGBA цветов
- ✅ Анализ font-family и font-size
- ✅ Статистика по частоте использования
- ✅ Генерация готового CSS шаблона
- ✅ Экспорт в JSON для дальнейшей обработки
