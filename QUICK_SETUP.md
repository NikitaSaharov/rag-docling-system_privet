# Быстрая настройка авторизации - 5 минут ⚡

## Шаг 1: Генерация секретных ключей

Запустите скрипт для генерации JWT и Flask секретов:

```powershell
python generate_secrets.py
```

Скопируйте полученные ключи и вставьте их в `.env.local` (строки 23-24).

---

## Шаг 2: Настройка Gmail App Password

### 2.1 Создание App Password

1. **Откройте**: https://myaccount.google.com/apppasswords

2. **Войдите** в ваш Google аккаунт

3. **Включите 2FA** (если не включена):
   - Перейдите: https://myaccount.google.com/security
   - Найдите "2-Step Verification" → Включите

4. **Вернитесь** на https://myaccount.google.com/apppasswords

5. **Создайте App Password**:
   - Select app: `Mail`
   - Select device: `Windows Computer` (или `Other`)
   - Нажмите **Generate**

6. **Скопируйте** 16-значный пароль (без пробелов)
   - Выглядит так: `abcd efgh ijkl mnop`
   - Используйте БЕЗ ПРОБЕЛОВ: `abcdefghijklmnop`

### 2.2 Обновите .env.local

Откройте `.env.local` и измените:

```bash
SMTP_USER=ваш-email@gmail.com          # Ваш Gmail адрес
SMTP_PASSWORD=abcdefghijklmnop         # App Password (БЕЗ пробелов!)
FROM_EMAIL=ваш-email@gmail.com         # Тот же email
```

**Пример:**
```bash
SMTP_USER=ivanov@gmail.com
SMTP_PASSWORD=xkcd1234abcd5678
FROM_EMAIL=ivanov@gmail.com
FROM_NAME=VectorStom RAG System
```

---

## Шаг 3: Проверка настройки

Запустите тестовый скрипт:

```powershell
python -c "from webapp.email_service import send_verification_email; send_verification_email('ваш-email@gmail.com', '123456')"
```

**Результат:**
- ✅ `Email sent to ваш-email@gmail.com` - всё работает!
- ❌ Ошибка - проверьте настройки SMTP

---

## Шаг 4: Запуск приложения

```powershell
python webapp/app.py
```

Откройте: http://localhost:5000

---

## Готово! 🎉

Теперь вы можете:
1. Нажать "Войти" → "Зарегистрироваться"
2. Ввести email и пароль
3. Получить код на почту
4. Начать пользоваться с историей чатов!

---

## Troubleshooting

### ❌ SMTP Authentication Error
**Причина:** Используется обычный пароль вместо App Password

**Решение:**
1. Убедитесь, что используете App Password (16 символов)
2. Проверьте, что 2FA включена в Google аккаунте
3. Попробуйте создать новый App Password

### ❌ Connection refused
**Причина:** Неверный SMTP_HOST или SMTP_PORT

**Решение:**
```bash
SMTP_HOST=smtp.gmail.com  # Без http://
SMTP_PORT=587             # Не 465!
```

### ❌ Email не приходит
**Проверьте:**
1. Спам-папку
2. Правильность email адреса
3. Лимиты отправки Gmail (500 писем/день)

---

## Альтернатива: Использовать другой SMTP сервис

Если Gmail не работает, можно использовать:

### Yandex Mail
```bash
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=ваш-email@yandex.ru
SMTP_PASSWORD=ваш-пароль
```

### Mail.ru
```bash
SMTP_HOST=smtp.mail.ru
SMTP_PORT=587
SMTP_USER=ваш-email@mail.ru
SMTP_PASSWORD=ваш-пароль
```

### Outlook/Hotmail
```bash
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=ваш-email@outlook.com
SMTP_PASSWORD=ваш-пароль
```

---

## Следующие шаги

После успешной настройки:
- ✅ Регистрация и вход работают
- ✅ Письма с кодами приходят
- ✅ История чатов сохраняется
- ✅ Админ-панель доступна на /admin

Подробная документация: `AUTH_SETUP.md`
