# Онлайн доступ (из любой сети)

Для доступа из Интернета используем **ngrok** - создает туннель к вашему ПК.

## Установка ngrok

### Вариант 1: Скачать exe
1. Зайдите на https://ngrok.com/download
2. Скачайте Windows версию
3. Распакуйте ngrok.exe в папку проекта

### Вариант 2: Через winget
```powershell
winget install ngrok.ngrok
```

### Вариант 3: Через Chocolatey
```powershell
choco install ngrok
```

## Регистрация (бесплатно)

1. Зарегистрируйтесь на https://ngrok.com (бесплатный аккаунт)
2. Получите authtoken на https://dashboard.ngrok.com/get-started/your-authtoken
3. Привяжите токен:
```powershell
ngrok config add-authtoken YOUR_TOKEN_HERE
```

## Запуск туннеля

### Простой способ
```powershell
ngrok http 5000
```

### С кастомным доменом (платно)
```powershell
ngrok http 5000 --domain=your-domain.ngrok.app
```

Вы получите URL вида: `https://xxxx-xxx-xxx-xxx.ngrok-free.app`

## Использование

1. **На ПК:** Запустите ngrok в отдельном окне PowerShell
2. **Откройте URL** который показал ngrok (например `https://a1b2-c3d4.ngrok-free.app`)
3. **Поделитесь URL** - можно открыть с любого устройства

## Автозапуск ngrok

Создайте файл `start_with_ngrok.ps1`:
```powershell
# Запускаем Docker
docker-compose up -d

# Ждем запуска
Start-Sleep -Seconds 10

# Запускаем ngrok
Start-Process ngrok -ArgumentList "http 5000"

Write-Host "✅ Система запущена!" -ForegroundColor Green
Write-Host "Откройте ngrok и скопируйте URL" -ForegroundColor Yellow
```

## Ограничения бесплатного ngrok

✅ **Включено:**
- Безлимитные туннели
- HTTPS автоматически
- 1 онлайн туннель одновременно

⚠️ **Ограничения:**
- URL меняется при каждом запуске
- 40 запросов/минуту (достаточно для тестирования)
- На странице показывается баннер ngrok

💰 **Платно ($8/мес):**
- Статический домен
- Без лимитов
- Без баннера
- Несколько туннелей

## Альтернативы ngrok

### 1. Cloudflare Tunnel (бесплатно, лучше ngrok)
```powershell
# Установка
winget install Cloudflare.cloudflared

# Запуск
cloudflared tunnel --url http://localhost:5000
```

**Плюсы:**
- Полностью бесплатно
- Нет лимитов
- Статический домен бесплатно
- Лучше безопасность

### 2. localtunnel (бесплатно, простой)
```powershell
# Установка (нужен Node.js)
npm install -g localtunnel

# Запуск
lt --port 5000
```

### 3. Tailscale (бесплатно, VPN)
- Создает VPN между устройствами
- Не нужен порт-форвардинг
- Безопаснее всего
- https://tailscale.com

## Безопасность

⚠️ **ВАЖНО:** Сейчас НЕТ авторизации!

Любой с URL может:
- Задавать вопросы вашей RAG системе
- Видеть документы в базе

### Как защитить:

#### 1. Базовая авторизация (быстро)
Добавьте в `webapp/app.py`:
```python
from functools import wraps
from flask import request, Response

def check_auth(username, password):
    return username == 'admin' and password == 'your_strong_password'

def authenticate():
    return Response('Login Required', 401, 
        {'WWW-Authenticate': 'Basic realm="Login Required"'})

def requires_auth(f):
    @wraps(f)
    def decorated(*args, **kwargs):
        auth = request.authorization
        if not auth or not check_auth(auth.username, auth.password):
            return authenticate()
        return f(*args, **kwargs)
    return decorated

# Добавьте @requires_auth ко всем роутам
@app.route('/')
@requires_auth
def index():
    return render_template('index.html')
```

#### 2. OAuth через ngrok (платно)
ngrok поддерживает OAuth с Google/GitHub

#### 3. IP whitelist (ngrok платно)
Разрешить доступ только с определенных IP

## Рекомендация

**Для постоянного использования:**
1. Используйте **Cloudflare Tunnel** (бесплатно + надежно)
2. Добавьте **базовую авторизацию**
3. Или разверните на VPS (см. DEPLOYMENT.md)

**Для тестирования:**
1. ngrok вполне подходит
2. Но не давайте URL посторонним без авторизации!
